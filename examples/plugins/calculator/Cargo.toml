[package]
name = "calculator"
version = "0.1.0"
edition = "2021"
description = "Calculator Claw plugin — math operations in WASM"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
