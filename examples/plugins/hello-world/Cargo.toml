[package]
name = "hello-world"
version = "0.1.0"
edition = "2021"
description = "Minimal Claw plugin example"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
